# Copy to `.env` for local development.
# Railway: Postgres → Connect → copy `DATABASE_URL` (use the **public** URL if connecting from Vercel).
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"
