# Railway config for the Next.js frontend (monorepo: set service Root Directory = frontend).
# In Railway service Settings → Config-as-code, point to: /frontend/railway.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
